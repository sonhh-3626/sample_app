<h1><%= t("users.mailer.password_reset.heading") %></h1>
<p><%= t("users.mailer.password_reset.follow_instructions") %></p>

<%= link_to t("users.mailer.password_reset.reset_link"), edit_password_reset_url(id: @user.reset_token, email: @user.email) %>

<p><%= t("users.mailer.password_reset.expiry_notice") %></p>

<p><%= t("users.mailer.password_reset.ignore_notice") %></p>
