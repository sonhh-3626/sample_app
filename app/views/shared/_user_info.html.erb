<%= link_to gravatar_for(current_user, size: Settings.gravatar_size_50), current_user %>
<h1><%= current_user.name %></h1>

<span><%= link_to t("users.show.view_my_profile"), current_user %></span>
<span><%= pluralize(current_user.microposts.count, t("microposts.form.micropost")) %></span>
